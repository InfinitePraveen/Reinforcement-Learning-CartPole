# Models

The trained Q-learning table is saved here as:

`q_table.npy`

It is a small NumPy array and can be loaded directly by the Flask application.

If the model is missing, run `notebooks/02_q_learning_training.ipynb` first.
